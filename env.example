# ============ 必填（生产建议必填）============
# 登录密码（不设置则默认不鉴权，任何人都能调用你的数据/LLM 配额）
APP_PASSWORD=your_password_here

# ============ Render Persistent Disk（推荐）============
# Render 控制台：Disks -> Mount Path 例如 /var/data
# Environment 里设置：DATA_DIR=/var/data
# 本地可不设置，默认写到项目目录下 ./data
DATA_DIR=/var/data

# ============ Gemini API（可选，但启用AI分析/聊天需要）============
# 二选一即可：优先使用 GEMINI_API_KEY；否则读取 GOOGLE_API_KEY
GEMINI_API_KEY=your_gemini_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here

# 默认模型（Gemini 3 最新示例）
GEMINI_MODEL=gemini-3-pro-preview
# thinking_level: low/high（medium 当前不支持）
GEMINI_THINKING_LEVEL=high
# 网络抖动/SSL握手失败时的重试次数（默认2）
GEMINI_MAX_RETRIES=2

# ============ FRED / TradingEconomics（可选）============
FRED_API_KEY=your_fred_key_here
TE_CLIENT_KEY=your_tradingeconomics_key_here
TE_CLIENT_SECRET=your_tradingeconomics_secret_here

# ============ 前端联调（可选）============
# 生产请写你的前端域名，逗号分隔；本地可留空（后端默认 allow_origins=*）
# 例：CORS_ALLOW_ORIGINS=https://your-frontend.onrender.com,https://www.yourdomain.com
CORS_ALLOW_ORIGINS=

# ============ 其他可选调优 ============
# Chat 注入 last_overall_report 的最大字符数（避免 system prompt 过长）
CHAT_LAST_REPORT_MAX_CHARS=7000

# TradingEconomics 缓存/熔断文件（不设置则默认写到 DATA_DIR 下）
# TE_CACHE_PATH=/var/data/cache_te_calendar.json
# TE_CIRCUIT_PATH=/var/data/cache_te_circuit.json
